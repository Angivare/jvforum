#!/usr/bin/env node
let app = require('../app')
  , http = require('http')

let server = http.createServer(app)
server.listen(process.env.PORT || 3000)

server.on('error', (error) => {
  throw error
})
